#+SETUPFILE: ../../../Setup/setupFile-3.org
#+TITLE: The vector classes

* Vector classes
  :PROPERTIES:
  :ID:       37ca8895-814e-4808-882f-fff1bde88bcc
  :END:

The hierachy is as follows:

** Vector_Crtp 

- Constructors 
#+begin_src cpp :eval never
Default_Vector()
Default_Vector(const N_TYPE n, const INCREMENT inc) 
Default_Vector(const N_TYPE n)
#+end_src

#+CALL: Cpp_Extract_Snippet("vector.hpp",tag="V")

#+RESULTS:
#+begin_SRC cpp :eval never
#+end_SRC

*** Default_Vector


   - Default_Vecto



  #+begin_src plantuml :file ./figures/vector_hierarchy.png :eval no-export
abstract Vector_Crtp
abstract Default_Vector_Crtp

class Default_Vector
class Default_Vector_View
class Default_Vector_Const_View

Vector_Crtp <|--- Default_Vector_Crtp
Default_Vector_Crtp  <|--- Default_Vector
Default_Vector_Crtp  <|--- Default_Vector_View
Default_Vector_Crtp  <|--- Default_Vector_Const_View

class Default_Vector {
 .. Constructors ..
+ Default_Vector()
+ Default_Vector(const N_TYPE n, const INCREMENT inc) 
+ Default_Vector(const N_TYPE n)
}

class Default_Vector_View {
 .. Constructors ..
+ Default_Vector_View(T* data)
+ Default_Vector_View(T* data, const N_TYPE n, const INCREMENT inc)  
}

class Default_Vector_Const_View {
 .. Constructors ..
+ Default_Vector_Const_View(const T* data)
+ Default_Vector_Const_View(const T* data, 
                          const N_TYPE n, const INCREMENT inc)  
}

  #+End_src


  #+RESULTS:
  [[file:./figures/vector_hierarchy.png]]


  #+begin_src plantuml :file ./figures/default_vector_crtp.png :eval no-export
class Default_Vector_Crtp {
 .. Methods ..
	+ element_type* data() mutable/const
	+ increment_type increment() const
	
	+ element_type& operator[](const size_t idx) mutable/const
	
	+ void map(const LAMBDA& lambda) mutable/const
	+ void map_indexed(const LAMBDA& lambda) mutable/const
	
	+ const storage_scheme_type& storage_scheme() const
}
  #+end_src

  #+RESULTS:
  [[file:./figures/default_vector_crtp.png]]
